<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="test.MainPage">
    <ContentPage.Content>
        <StackLayout>
            <Frame BackgroundColor="Red" Padding="24" CornerRadius="0">
                <Label Text="Hello World" HorizontalTextAlignment="Center" TextColor="White" FontSize="36"/>
            </Frame>
            <Button Text="Click Me" />

            <Label Text="Mijn eerste Android app" FontSize="Title" Padding="30,10,30,10"/>
            <Label Text="Deze android app is gebouwd met Xamarin.forms waarbij alleen het MainPage.xml bestand is aangepast. " FontSize="16" Padding="30,0,30,0"/>
            <Label FontSize="16" Padding="30,24,30,0">
                <Label.FormattedText>
                    <FormattedString>
                        <FormattedString.Spans>
                            <Span Text="De afbeelding is toegevoegd met image tag  met het atribuut Source, waarbij de afbeelding in de map Resources/drawable is toegevoegd. let daarbij op dat de afbeelding als property buildAction een androidResource is " FontSize="16"/>


                        </FormattedString.Spans>
                    </FormattedString>
                </Label.FormattedText>

            </Label>


        </StackLayout>
    </ContentPage.Content>
</ContentPage>
    